# Example Project

This project is configured with [iso-harness](https://github.com/razroo/iso-harness).
One source of truth lives in `iso/`; each coding-agent harness reads the
generated config that matches its own conventions.

## Guidelines

- Write small, focused changes.
- Run tests before committing.
- Prefer editing existing files to creating new ones.
